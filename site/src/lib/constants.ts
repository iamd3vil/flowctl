export const FLOWS_PER_PAGE = 50;
export const DEFAULT_PAGE_SIZE = 50;

// Injected at build time
export const APP_VERSION = import.meta.env.VITE_VERSION || "dev";
export const APP_COMMIT = import.meta.env.VITE_COMMIT || "";
export const APP_DATE = import.meta.env.VITE_DATE || "";
